# Github项目公开前的信息泄露检查

## **旧版本历史**

- `git clone` → 拿到完整历史，能翻出每个曾存在的文件版本，可能会泄露个人信息
- GitHub 网页 Download ZIP → 只有当前快照，无历史

GitHub 上一旦推过，就算 force push 覆盖，**旧提交的 URL 短期内仍可打开**（直到垃圾回收）。

所以想彻底纯净，最稳的是：删掉远端仓库 → 新建空仓库 → 只推干净提交。

## 六个泄露点

### 1. Git 作者邮箱

每个提交都带元数据 `user.email`。通过 `git log` 或点 GitHub Commits 页面直接可见。

```bash
# 检查（只看分类，不打印值）
git log --all --format='%ae' | sort -u
```

对策：用 GitHub 提供的 noreply 邮箱，作者名用网名而非真名。

```bash
git config user.email "你的ID+用户名@users.noreply.github.com"
```

### 2. 本机绝对路径与系统用户名

内部交接文档、构建脚本、README 里的 `C:\Users\<用户名>\...`，会暴露系统账号名和本机目录结构。

```bash
# 扫描全仓库历史，找到硬编码写死本机用户路径的文件
git grep -lEI 'C:\\Users\\|/home/|/Users/' $(git rev-list --all)
```

### 3. 凭据与 .env

授权码、API key、密码硬编码或误提交配置文件。

```bash
# 在 Git 所有历史提交里搜索疑似硬编码密钥或密码的文件
git grep -lEI 'password[[:space:]]*[:=][[:space:]]*"[^"]{6,}"|api[_-]?key|secret' $(git rev-list --all)
```

对策：真实配置放系统凭据存储（Windows 凭据管理器 / macOS Keychain），`.gitignore` 排除 `.env*`。

### 4. IDE 与草稿目录

`.idea/`、`.vscode/`、开发笔记目录。含本机路径，且对外无价值。

```gitignore
.idea/
.vscode/
```

### 5. 代码注释与提交说明里的真人信息

真实姓名、私人邮箱、内部系统名、同事名字。

### 6. 缺少 LICENSE

没有许可证 = 默认版权保留，别人不能放心用也不能放心改。常见 MIT（最宽松）或 GPL。

## 公开前操作清单

```text
□ 作者邮箱换成 noreply
□ 扫全历史：路径 / 凭据 / 邮箱
□ 删除内部交接、调研类文档
□ .gitignore 补 IDE 与草稿目录
□ 加 LICENSE
□ README 重写（对外口径，非开发笔记）
□ 做成单一初始提交
□ 远端仓库删除重建后再推
```
